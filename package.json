{
  "name": "bicycle-webapp",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "concurrently \"npm run build-and-start-backend\" \"npm run start --prefix frontend/hobigowes-app\"",
    "build-and-start-backend": "npm run build --prefix backend && npm run start --prefix backend",
    "install-all": "npm install --prefix backend && npm install --prefix frontend/hobigowes-app"
  },
  "devDependencies": {
    "concurrently": "^6.2.0"
  }
}
